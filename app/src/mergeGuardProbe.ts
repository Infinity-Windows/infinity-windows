// Throwaway file: deliberately fails `tsc -b` to prove the new master ruleset
// refuses to merge a red pull request. Deleted immediately after the test.
export const probe: number = "this is not a number";
