// Throwaway: deliberately fails `tsc -b`. Proves the master ruleset refuses a red
// pull request even for a repository admin. Branch is deleted after the test.
export const probe: number = "definitely not a number";
