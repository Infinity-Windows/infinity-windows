node_modules/
dist/
.env
.DS_Store
supabase/.temp/
.vapid-keys.local.json

# Playwright run artefacts. The layout screenshots in app/e2e/__screenshots__ are
# committed on purpose; the foreman ones are not, because they are pictures of
# live customer-database state taken against a real login.
app/e2e/test-results/
app/e2e/test-results-foreman/
app/e2e/__screenshots__/foreman/
app/playwright-report/
app/blob-report/
app/.playwright/

# The test login's password (docs/test-account.md). Local only, never committed.
.secrets/
