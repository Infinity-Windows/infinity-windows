# Role access matrix

_Auto-generated from `app/src/lib/nav.ts`. Do not edit by hand — run `npx tsx app/scripts/gen-role-access.ts` (or `UPDATE_DOCS=1 npx vitest run roleAccessDoc`) to regenerate. A snapshot test fails if this file drifts from the code._

## Installer

**Bottom bar (phone):**

- Menu (opens the drawer)
- Today (`/`)
- Scan (`/scan`)
- Clock (time tracking sheet)
- Ask (`/ask`)

**Menu drawer:**

- My Work (`/`)
- Hub (`/warehouse`)
- My Schedule (`/my-schedule`)
- Clock in / out (action)
- My timecard (`/timecard`)
- Travel (`/travel`)
- Learn (`/learn`)
- Points (`/points`)
- Memo review (`/review`)
- Safety (`/safety`)
- Notifications (`/notifications`)
- Settings (`/settings`)

**Can reach (all 23 allowed destinations):**

- Home (`/`)
- Time (`/clock`)
- Learn (`/learn`)
- Points (`/points`)
- Safety (`/safety`)
- Scan (`/scan`)
- Cycle count (`/count`)
- Warehouse (`/warehouse`)
- Jobs (`/projects`)
- Ask (`/ask`)
- Notifications (`/notifications`)
- Search (`/search`)
- Memo review (`/review`)
- My Schedule (`/my-schedule`)
- Travel (`/travel`)
- Timecard (`/timecard`)
- Photos & receipts (`/photos`)
- Completed installs (`/completed-installs`)
- Milestones (`/milestones`)
- First Pane (`/first-pane`)
- Toolbox talk history (`/toolbox-history`)
- Profile (`/profile`)
- View public site (`/public-site`)

**Blocked:**

- Team (`/team`)
- Issues (`/issues`)
- Service (`/service`)
- Quality (`/qc`)
- Analytics (`/analytics`)
- Roster (`/crew`)
- Training (`/training`)
- Receive (`/receive`)
- Slot labels (`/labels`)
- Catalog (`/catalog`)
- Supplies (`/supplies`)
- AI Knowledge (`/knowledge`)
- Scheduling (`/scheduling`)
- Vehicles (`/vehicles`)
- Heartbeat (`/heartbeat`)
- Admin (`/admin`)
- Crew access (`/access`)
- Cost codes (`/cost-codes`)
- Cost (`/costing`)
- AI spend (`/ai-spend`)
- Daily logs (`/daily-logs`)
- Conditions (`/conditions`)
- Contacts (`/contacts`)

## Foreman

**Bottom bar (phone):**

- Menu (opens the drawer)
- Jobs (`/projects`)
- Capture (quick-capture sheet)
- Clock (time tracking sheet)
- Ask (`/ask`)

**Menu drawer:**

- Home (`/`)
- My Work (`/my-work`)
- Jobs (`/projects`)
- Photos & receipts (`/photos`)
- Clock in / out (action)
- My timecard (`/timecard`)
- Analytics (`/analytics`)
- Hub (`/warehouse`)
- Scan (`/scan`)
- Cycle count (`/count`)
- Receive (`/receive`)
- Slot labels (`/labels`)
- Catalog (`/catalog`)
- Supplies (`/supplies`)
- Issues (`/issues`)
- Service (`/service`)
- Quality (`/qc`)
- My Schedule (`/my-schedule`)
- Travel (`/travel`)
- Team (`/team`)
- Roster (`/crew`)
- Learn (`/learn`)
- Training (`/training`)
- Points (`/points`)
- Memo review (`/review`)
- Safety (`/safety`)
- Ask (`/ask`)
- Notifications (`/notifications`)
- Settings (`/settings`)

**Can reach (all 37 allowed destinations):**

- Home (`/`)
- Time (`/clock`)
- Learn (`/learn`)
- Points (`/points`)
- Safety (`/safety`)
- Scan (`/scan`)
- Cycle count (`/count`)
- Warehouse (`/warehouse`)
- Jobs (`/projects`)
- Ask (`/ask`)
- Notifications (`/notifications`)
- Search (`/search`)
- Memo review (`/review`)
- My Schedule (`/my-schedule`)
- Travel (`/travel`)
- Timecard (`/timecard`)
- Team (`/team`)
- Issues (`/issues`)
- Service (`/service`)
- Quality (`/qc`)
- Analytics (`/analytics`)
- Roster (`/crew`)
- Training (`/training`)
- Receive (`/receive`)
- Slot labels (`/labels`)
- Catalog (`/catalog`)
- Supplies (`/supplies`)
- Photos & receipts (`/photos`)
- Daily logs (`/daily-logs`)
- Completed installs (`/completed-installs`)
- Milestones (`/milestones`)
- First Pane (`/first-pane`)
- Toolbox talk history (`/toolbox-history`)
- Conditions (`/conditions`)
- Contacts (`/contacts`)
- Profile (`/profile`)
- View public site (`/public-site`)

**Blocked:**

- AI Knowledge (`/knowledge`)
- Scheduling (`/scheduling`)
- Vehicles (`/vehicles`)
- Heartbeat (`/heartbeat`)
- Admin (`/admin`)
- Crew access (`/access`)
- Cost codes (`/cost-codes`)
- Cost (`/costing`)
- AI spend (`/ai-spend`)

## Supervisor

**Bottom bar (phone):**

- Menu (opens the drawer)
- Jobs (`/projects`)
- Capture (quick-capture sheet)
- Clock (time tracking sheet)
- Ask (`/ask`)

**Menu drawer:**

- Home (`/`)
- My Work (`/my-work`)
- Jobs (`/projects`)
- Photos & receipts (`/photos`)
- Clock in / out (action)
- My timecard (`/timecard`)
- Cost codes (`/cost-codes`)
- Analytics (`/analytics`)
- Heartbeat (`/heartbeat`)
- Hub (`/warehouse`)
- Scan (`/scan`)
- Cycle count (`/count`)
- Receive (`/receive`)
- Slot labels (`/labels`)
- Catalog (`/catalog`)
- Supplies (`/supplies`)
- Issues (`/issues`)
- Service (`/service`)
- Quality (`/qc`)
- Scheduling (`/scheduling`)
- Vehicles (`/vehicles`)
- My Schedule (`/my-schedule`)
- Travel (`/travel`)
- Team (`/team`)
- Roster (`/crew`)
- Crew access (`/access`)
- Learn (`/learn`)
- Training (`/training`)
- Points (`/points`)
- Memo review (`/review`)
- Safety (`/safety`)
- Ask (`/ask`)
- AI Knowledge (`/knowledge`)
- Notifications (`/notifications`)
- Settings (`/settings`)
- Admin (`/admin`)

**Can reach (all 44 allowed destinations):**

- Home (`/`)
- Time (`/clock`)
- Learn (`/learn`)
- Points (`/points`)
- Safety (`/safety`)
- Scan (`/scan`)
- Cycle count (`/count`)
- Warehouse (`/warehouse`)
- Jobs (`/projects`)
- Ask (`/ask`)
- Notifications (`/notifications`)
- Search (`/search`)
- Memo review (`/review`)
- My Schedule (`/my-schedule`)
- Travel (`/travel`)
- Timecard (`/timecard`)
- Team (`/team`)
- Issues (`/issues`)
- Service (`/service`)
- Quality (`/qc`)
- Analytics (`/analytics`)
- Roster (`/crew`)
- Training (`/training`)
- Receive (`/receive`)
- Slot labels (`/labels`)
- Catalog (`/catalog`)
- Supplies (`/supplies`)
- AI Knowledge (`/knowledge`)
- Scheduling (`/scheduling`)
- Vehicles (`/vehicles`)
- Heartbeat (`/heartbeat`)
- Admin (`/admin`)
- Crew access (`/access`)
- Cost codes (`/cost-codes`)
- Photos & receipts (`/photos`)
- Daily logs (`/daily-logs`)
- Completed installs (`/completed-installs`)
- Milestones (`/milestones`)
- First Pane (`/first-pane`)
- Toolbox talk history (`/toolbox-history`)
- Conditions (`/conditions`)
- Contacts (`/contacts`)
- Profile (`/profile`)
- View public site (`/public-site`)

**Blocked:**

- Cost (`/costing`)
- AI spend (`/ai-spend`)

## Owner

**Bottom bar (phone):**

- Menu (opens the drawer)
- Jobs (`/projects`)
- Capture (quick-capture sheet)
- Clock (time tracking sheet)
- Ask (`/ask`)

**Menu drawer:**

- Home (`/`)
- My Work (`/my-work`)
- Jobs (`/projects`)
- Photos & receipts (`/photos`)
- Clock in / out (action)
- My timecard (`/timecard`)
- Cost codes (`/cost-codes`)
- Cost (`/costing`)
- Analytics (`/analytics`)
- Heartbeat (`/heartbeat`)
- Hub (`/warehouse`)
- Scan (`/scan`)
- Cycle count (`/count`)
- Receive (`/receive`)
- Slot labels (`/labels`)
- Catalog (`/catalog`)
- Supplies (`/supplies`)
- Issues (`/issues`)
- Service (`/service`)
- Quality (`/qc`)
- Scheduling (`/scheduling`)
- Vehicles (`/vehicles`)
- My Schedule (`/my-schedule`)
- Travel (`/travel`)
- Team (`/team`)
- Roster (`/crew`)
- Crew access (`/access`)
- Learn (`/learn`)
- Training (`/training`)
- Points (`/points`)
- Memo review (`/review`)
- Safety (`/safety`)
- Ask (`/ask`)
- AI Knowledge (`/knowledge`)
- AI spend (`/ai-spend`)
- Notifications (`/notifications`)
- Settings (`/settings`)
- Admin (`/admin`)

**Can reach (all 46 allowed destinations):**

- Home (`/`)
- Time (`/clock`)
- Learn (`/learn`)
- Points (`/points`)
- Safety (`/safety`)
- Scan (`/scan`)
- Cycle count (`/count`)
- Warehouse (`/warehouse`)
- Jobs (`/projects`)
- Ask (`/ask`)
- Notifications (`/notifications`)
- Search (`/search`)
- Memo review (`/review`)
- My Schedule (`/my-schedule`)
- Travel (`/travel`)
- Timecard (`/timecard`)
- Team (`/team`)
- Issues (`/issues`)
- Service (`/service`)
- Quality (`/qc`)
- Analytics (`/analytics`)
- Roster (`/crew`)
- Training (`/training`)
- Receive (`/receive`)
- Slot labels (`/labels`)
- Catalog (`/catalog`)
- Supplies (`/supplies`)
- AI Knowledge (`/knowledge`)
- Scheduling (`/scheduling`)
- Vehicles (`/vehicles`)
- Heartbeat (`/heartbeat`)
- Admin (`/admin`)
- Crew access (`/access`)
- Cost codes (`/cost-codes`)
- Cost (`/costing`)
- AI spend (`/ai-spend`)
- Photos & receipts (`/photos`)
- Daily logs (`/daily-logs`)
- Completed installs (`/completed-installs`)
- Milestones (`/milestones`)
- First Pane (`/first-pane`)
- Toolbox talk history (`/toolbox-history`)
- Conditions (`/conditions`)
- Contacts (`/contacts`)
- Profile (`/profile`)
- View public site (`/public-site`)

**Blocked:**

- _(nothing — full access)_
